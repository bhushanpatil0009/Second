public with sharing class AdvancedConcept2
 {
		public void question1()
		{	
		Set<lead> leadSet = new Set<lead>([SELECT id,name FROM lead]);	
		Set<lead> leadresult = new Set<lead>();
		for (set<lead> leadsets : leadset)
		{
			leadresult.add(leadsets.id);
			system.debug(leadresult);
		}
		
		}
}