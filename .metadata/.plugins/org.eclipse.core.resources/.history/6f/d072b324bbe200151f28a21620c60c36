public with sharing class AdvancedConcept2
 {
		public void question1()
		{	
		Set<lead> leadSet = new Set<lead>([SELECT name FROM lead]);	
		Set<lead> leadSet1 = new Set<lead>();
		for(lead ld : leadSet)
		{
			leadset1.add(ld.id);
		}
		system.debug(leadset1);
		}
}